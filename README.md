# JokeAPI
A practice project in Python, utilizing an API and testing

### Installation
tbc


### Pytest testing
Open the folder JokeAPI

Open a CLI (Command Line Interface) window on the root directory of the JokeAPI project

Execute the following command in CLI:
```python -m pytest```